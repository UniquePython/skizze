[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "skizze"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
skizze = "skizze.skizze_cli:main"